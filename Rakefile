require 'neography'
require 'neography/tasks'

namespace :neo4j do
  task :create do
   # Neo = Neography::Rest.new(ENV['GRAPHENEDB_URL'] || "http://localhost:7474")
 #Neography::Rest.new("http://username:password@yourserver.sb02.stations.graphenedb.com:24789")
  Neo = Neography::Rest.new("http://AlumnosDB:sYvd8vGnswmF4bpjIVZZ@alumnosdb.sb02.stations.graphenedb.com:24789")    
#cypher = File.read('data/movies.cyp').split(';')
    #neo.commit_transaction(cypher)
    #cypher = contents = File.read('data/upgrade.cyp').split(';')
    #neo.commit_transaction(cypher)
  end
end
